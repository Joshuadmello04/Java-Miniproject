package Home;

public class Main {
    public static void main(String[] args) {
        // Create an instance of the LoginPage class (assuming the LoginPage class is in
        // the Home package)
        LoginPage loginPage = new LoginPage();

        // Set the visibility of the login page to true, making it visible to the user
        loginPage.setVisible(true);
    }
}
